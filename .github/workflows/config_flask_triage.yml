name: config_flask triage
on:
  issues:
    types: [opened]
jobs:
  commentOnNewIssues:
    name: config_flask triage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: config_flask triage
        uses: actions/github@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: comment "[config_flask] should be supported for Python 2.7, 3.5, 3.6, 3.7, 3.8, 3.9"
